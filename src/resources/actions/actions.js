export const SET_VALUE = (value) => ({
    type: 'SET_VALUE',
    payload: value,
  });

  export const SET_CHART = (value) => ({
    type: 'SET_CHART',
    payload: value,
  });

  export const SET_MAP = (value) => ({
    type: 'SET_MAP',
    payload: value,
  });
